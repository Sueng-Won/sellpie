<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NoticeMapper">
	<select id="selectNoticeList" resultType="Notice">
		SELECT NNO, NTITLE, NCONTENT, NWRITER, NCOUNT, NDATE, ATTACH
		FROM NOTICE
		ORDER BY NNO
	</select>
	<select id="selectNotice" parameterType="int" resultType="Notice">
		SELECT NNO, NTITLE, NCONTENT, NWRITER, NCOUNT, NDATE, ATTACH
		FROM NOTICE
		WHERE NNO = #{nNo}
	</select>
	<update id="updateNotice" parameterType="Notice">
		UPDATE NOTICE SET
		NTITLE = #{nTitle},
		NCONTENT = #{nContent},
		ATTACH = #{attach}
		WHERE NNO = #{nNo}
	</update>
	<delete id="deleteNotice" parameterType="int">
		DELETE NOTICE
		WHERE NNO = #{nNo}
	</delete>
	<insert id="insertNotice" parameterType="Notice">
		INSERT INTO NOTICE
		VALUES(SEQ_NNO.NEXTVAL,#{nTitle},#{nContent},#{nWriter},0,SYSDATE,#{attach})
	</insert>
	<update id="updateNoticeCount" parameterType="int">
		UPDATE NOTICE SET
		NCOUNT = NCOUNT+1
		WHERE NNO = #{nNo}
	</update>
</mapper>